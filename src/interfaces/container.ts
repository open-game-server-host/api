export interface CreateContainerData {
    appId: string;
    variantId: string;
    versionId: string;
    free: boolean;
    name: string;
    regionId: string;
    runtime: string;
    segments: number;
    userId: string;
}